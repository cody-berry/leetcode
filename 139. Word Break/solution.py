class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        # for each word in wordDict...

            # if s is equal to that word, return true. 

            # check if the start of s is equal to that word.

                # call wordBreak on s without that word and the wordDict still.
                # if that's true, return true. otherwise, go on.

        return False
